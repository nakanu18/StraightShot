import React from 'react';
import { View } from 'react-native';

class RoundEditorScreen extends React.Component {

    // Properties

    state = {
        selectedRoundId: null,
        results: [],
    };

    // Life cycle methods

    componentWillMount() {
    }

    // Rendering

    render() {
        return (
            <View />
        );
    }

}

export default RoundEditorScreen;
